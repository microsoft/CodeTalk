import * as ACTION_TYPES from "../constants/actions";

const initialState = {};

export default function alertReducer(state = initialState, action) {
  switch (action.type) {
      case ACTION_TYPES.SEND_LAST_UPDATE:
        
        break;
      
  }
  return state;
}
